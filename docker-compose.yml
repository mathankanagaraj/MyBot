services:
  tradingbot:
    build: .
    container_name: intraday_options_bot

    env_file: .env
    environment:
      - IB_HOST=host.docker.internal
    volumes:
      - ./logs:/app/logs
      - ./audit:/app/audit
    restart: "no" # Systemd manages restarts via timer and health check
